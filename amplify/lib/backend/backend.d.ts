import { backend } from '../backend';
export default backend;
/**
 * Developer notes:
 * This file is maintained for compatibility with older Amplify CLI versions.
 * For new projects, the main configuration should be in '../backend.ts'.
 */ 
